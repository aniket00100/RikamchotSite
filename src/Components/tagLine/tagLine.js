import React from "react";

import "./tagLine.css";

const tagLine = () => {
  return (
    <div>
      <h1 className="text">
        "This is where the editor will add an embarassing one liner intro for
        the profile"
      </h1>
    </div>
  );
};

export default tagLine;
